[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "bms-skills-cli"
version = "0.2.1"
description = "A minimal CLI to manage agent skills from GitHub releases."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "typer",
    "rich",
    "questionary",
    "requests",
]

[dependency-groups]
dev = [
    "pyright",
    "pytest",
]

[project.scripts]
bms-skills = "bms_skills.cli:app"

[tool.uv]
package = true

[tool.hatch.build.targets.wheel]
packages = ["bms_skills"]
